"""一次性：把 framework.md 里契约部分的"接口"统一为"契约"。

约定（v0.4 定）：
  · 六个 I1~I6 一律称 **契约**（"接口"暗示分层，而契约的要点是"谁能读写"）
  · `Ix-n` 是**契约条款编号**，不是"接口编号"
  · 保留：`接口网关`（产品/模块名）、`OpenAPI 接口描述`（外部标准用语）、"契约先于数据"行内的"接口"
"""
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent.parent
FW = ROOT / "docs" / "framework.md"

RULES = [
    # §14 契约总表
    ("| **I1** | **语义接口** |", "| **I1** | **语义契约** |"),
    ("| **I2** | **事实接口** |", "| **I2** | **事实契约** |"),
    ("| **I3** | **查询接口** |", "| **I3** | **查询契约** |"),
    ("| **I4** | **动作接口** |", "| **I4** | **动作契约** |"),
    ("| **I5** | **接入接口** |", "| **I5** | **接入契约** |"),
    ("| **I6** | **发现能力** |", "| **I6** | **发现契约** |"),
    ("### 14.1 接口 × 五要素：谁负责读哪些要素",
     "### 14.1 契约 × 五要素：谁负责读哪些要素"),
    ("| 归 I5 接入接口，**不占要素** |", "| 归 I5 接入契约，**不占要素** |"),
    ("归 **I5 接入接口**，用 Ossie", "归 **I5 接入契约**，用 Ossie"),
    ("| **接口的调用者**（人 / Agent）", "| **契约的调用者**（人 / Agent）"),
    ("且可由 I6 发现接口兜底", "且可由 I6 发现契约兜底"),
    ("| 1 | **五个要素全部是\"读多写无\"** | 定义的变更**不经过任何接口**，走治理流程（§1.2）。这就是\"不开放就地修改\"在接口层的体现 |",
     "| 1 | **五个要素全部是\"读多写无\"** | 定义的变更**不经过任何契约**，走治理流程（§1.2）。这就是\"不开放就地修改\"在契约层的体现 |"),
    ("| 3 | **映射不是要素，是一个接口** |", "| 3 | **映射不是要素，是一条契约** |"),
    ("但接口清单里**没有一个\"改定义\"的接口**", "但契约清单里**没有一个\"改定义\"的契约**"),
    ("| **I1** | **语义接口** | 所有层 → L3", "| **I1** | **语义契约** | 所有层 → L3"),
    ("| **I2** | **事实接口** | L4/L5 → L2", "| **I2** | **事实契约** | L4/L5 → L2"),
    ("| **I3** | **查询接口** | L6 → L4", "| **I3** | **查询契约** | L6 → L4"),
    ("| **I4** | **动作接口** | L6 → L5", "| **I4** | **动作契约** | L6 → L5"),
    ("| **I5** | **接入接口** | 外部 → L1", "| **I5** | **接入契约** | 外部 → L1"),
    ("**全部六个接口 + 全部治理机制**", "**全部六个契约 + 全部治理机制**"),
    ("| **接口** | 直接采用 | **本框架需要补的** |", "| **契约** | 直接采用 | **本框架需要补的** |"),
    ("| 查询接口**永不产生写入** |", "| 查询契约**永不产生写入** |"),
    ("| 接口描述 | **OpenAPI** | 有标准 |", "| 契约描述（OpenAPI） | **OpenAPI** | 有标准 |"),
    ("### 25.3 I1 语义接口", "### 25.3 I1 语义契约"),
    ("### 25.4 I2 事实接口", "### 25.4 I2 事实契约"),
    ("### 25.5 I3 查询接口", "### 25.5 I3 查询契约"),
    ("### 25.6 I4 动作接口", "### 25.6 I4 动作契约"),
    ("### 25.7 I5 接入接口", "### 25.7 I5 接入契约"),
    ("### 25.8 I6 发现", "### 25.8 I6 发现契约"),
    ("| AG-1 | 所有接口的返回值", "| AG-1 | 所有契约的返回值"),
    ("Agent 会无意识烧钱，接口必须能限", "Agent 会无意识烧钱，契约必须能限"),
    ("`Ix-n`（接口）", "`Ix-n`（契约）"),
    ("Level 2  可写     + 动作接口（I4）", "Level 2  可写     + 动作契约（I4）"),
    ("Level 1  可读     + 事实 + 查询 + 发现接口", "Level 1  可读     + 事实 + 查询 + 发现契约"),
    ("Level 0  可声明   + 语义接口（I1）", "Level 0  可声明   + 语义契约（I1）"),
    ("因为它们是**机器可读的接口签名**", "因为它们是**机器可读的契约签名**"),
]

t = FW.read_text(encoding="utf-8")
n = 0
for old, new in RULES:
    if old in t:
        c = t.count(old)
        t = t.replace(old, new)
        n += c
    else:
        print(f"  ⚠ 锚点未命中：{old[:48]}…")
FW.write_text(t, encoding="utf-8", newline="\n")
print(f"共 {n} 处")

left = [f"{i+1}: {l.strip()}" for i, l in enumerate(t.split("\n")) if "接口" in l]
print(f"剩余含『接口』的行 {len(left)} 条：")
for x in left:
    print("   ", x[:110])
