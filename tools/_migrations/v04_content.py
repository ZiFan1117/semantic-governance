"""v0.4 重构用的正文常量。由 restructure_v04.py 引用。"""

NEW_HEAD = r"""# 面向业务的操作型本体：纲领性框架（草案 v0.4）
<!-- clauses: I1 I2 I3 I4 I5 I6 G L ELE AG LD -->

> **理念**：语义治理（Semantic Governance）
>
> **形态**：**中央式**——一套权威语义，所有系统向它对齐
>
> **v0.4 相对 v0.3 的修正**（2026-09-17）：
> 1. **删掉 L1~L6「分层视图」**——它没有公开出处（是把 Palantir 的后端服务反推成"层"、再补的理论说辞），
>    与 §0 的架构图重复，还制造了"三套轴抢主轴"的混乱
> 2. **TBox / ABox 从"层"改回"角色"**——它们是"这句话在讲类型还是讲事实"，不占架构位置（§5.2）
> 3. **新增 §5「四个正交维度」**：五要素（内容）· TBox/ABox（角色）· Ossie（格式）· 契约（通道），一次讲清谁管什么
> 4. **五要素逐个标出处**：对象·关系·规则**有 Ossie 官方槽位**；**策略与动作是原创**（Ossie 无，且无公开规范可依）
> 5. **新增 §11「纪律条款」**（`LD-1`~`LD-7`）：接入侧是条件能力 · 定义与事实分属不同单元 · 契约不得增设第六个要素
> 6. **保留 §14 起的全部编号**（契约、模块、合规、规范条款、开放问题）——编号不变，全仓引用不断
>
> **v0.3 相对 v0.2**：六个可声明原语 → 五要素；派生不再占要素位（声明归规则、产物归对象）；
> 「约束」降为载体名；映射移出要素表。
>
> 背景分析见 [`rationale.md`](rationale.md)

---

## 目录

**第零部分 · 理念与定位**
- [0. 语义治理是什么](#0-语义治理是什么)
- [0.5 【关键】形态选择：中央式权威及其代价](#05-关键形态选择中央式权威及其代价)

**第一部分 · 治理维度**（一等公民）
- [1. 治理的六件事](#1-治理的六件事)
- [2. 三类角色与权责](#2-三类角色与权责)
- [3. 三种剖面（Profile）](#3-三种剖面profile)

**第二部分 · 五要素** ★主轴
- [4. 术语表](#4-术语表)
- [5. 四个正交维度：别把它们混成一锅](#5-四个正交维度别把它们混成一锅)
- [6. 对象](#6-对象)
- [7. 关系](#7-关系)
- [8. 规则](#8-规则)
- [9. 策略](#9-策略)
- [10. 动作](#10-动作)
- [11. 纪律条款](#11-纪律条款)

**第三部分 · 契约视图**（谁能读写；**不是主轴**）
- [14. 六个契约](#14-六个契约)
- [15. I1 语义契约](#15-i1-语义契约)
- [16. I2 事实契约](#16-i2-事实契约)
- [17. I3 查询契约](#17-i3-查询契约)
- [18. I4 动作契约](#18-i4-动作契约)
- [19. I5 接入契约](#19-i5-接入契约)
- [20. I6 发现契约](#20-i6-发现契约)
- [21. 契约 × 现有标准对照](#21-契约--现有标准对照)

**第四部分 · 模块视图**
- [22. 模块清单与可替换性](#22-模块清单与可替换性)

**第五部分 · 合规等级**
- [23. 五级阶梯 × 三种剖面](#23-五级阶梯--三种剖面)
- [24. 验收标准](#24-验收标准)

**第六部分 · 规范条款（Normative）**
- [25. MUST / MUST NOT / SHOULD 清单](#25-must--must-not--should-清单)

**第七部分 · 实施指南（Informative）**
- [26. 四阶段路线](#26-四阶段路线)
- [27. Agent-first 设计原则](#27-agent-first-设计原则)

**第八部分 · 开放问题**
- [28. 尚未解决的问题](#28-尚未解决的问题)

**附录**
- [附录 A · 一页速查](#附录-a--一页速查)
- [附录 B · 从 v0.3 到 v0.4 的迁移对照](#附录-b--从-v03-到-v04-的迁移对照)
"""

NEW_S0 = r"""# 第零部分 · 理念与定位

---

## 0. 语义治理是什么

### 0.1 问题陈述

**现状**：企业的业务语义散落在应用代码、报表定义、数仓模型、文档里。后果是——

- 同一个"客户"在四个系统里有四个定义
- 同一个"营收"在四个看板上有四个数字
- 改一个规则要改几十个地方，而且改不全
- Agent 拿到互相冲突的业务逻辑，只能胡说

**Palantir 的解法**：把业务语义抽出来，做成一层**可声明、可校验、可执行、可交换的契约**，
并在这层契约之上提供**受控的修改能力**（动作）——即"语义层 + 动能层"。

**本框架要给出一个通用、公开、可开源的等价物。**

### 0.2 为什么叫「语义治理」而不是「本体论」

| 词 | 隐含的重心 | 问题 |
|---|---|---|
| 本体论 | 建模 | 听起来像学术活动，做完就完了 |
| 知识图谱 | 数据 | 听起来像存储选型 |
| **语义治理** | **权责 + 契约 + 技术** | ✅ 点出了"这是一件持续治理的事" |

**"治理"三个字要求框架必须回答：谁有权定义、怎么改、有分歧怎么办、怎么退出。**
一个不回答这些问题的"语义"框架，只是又一份 schema 规范。

### 0.3 核心命题

> **把业务语义从应用代码里抽出来，做成一份可声明、可校验、可执行、可交换的权威契约；
> 规定六个契约，让所有读写都必须经过它；
> 并用一套治理机制保证这份契约能持续演进而不失控。**

### 0.4 【架构】东西都放在哪

```
     ┌──────────────────────────────────────────────────────┐
     │   使用者：人 ／ 智能体                                │   ← 在外：调用方
     │   （不拥有数据、不内置规则，只走契约）                 │
     └───────────────────────▲──────────────────────────────┘
                             │ 一整套契约
                             │ 语义 · 事实 · 查询 · 动作 · 接入 · 发现
     ┌───────────────────────┴──────────────────────────────┐
     │                 本 体 空 间                           │   ← 主体
     │                                                       │
     │   五个要素（各一份，统一管理，不重复定义）             │
     │     对象 · 关系 · 规则 · 策略 · 动作                  │
     │                                                       │
     │   ★ 定义侧（TBox 角色）：允许什么、该怎样              │
     └───────────────────────▲──────────────────────────────┘
                             │ 数据上行 ／ 指令下行
                             │ 经统一映射：字段 → 对象与事实
     ┌───────────────────────┴──────────────────────────────┐
     │                 数 据 来 源                           │   ← 在外：从哪来
     │   （外部系统 · 设备 · 数据库）                        │
     │   ★ 事实侧（ABox 角色）：实际是什么                    │
     └──────────────────────────────────────────────────────┘

        本体构建与管理工具 ──指向──▶ 本体空间
        （建 · 改 · 管 · 发布；不与本体空间并列，也不连使用者）
```

**这张图要读出三件事：**

| # | 要点 |
|---|---|
| 1 | **本体空间是主体**，五个要素在里面。使用者和数据来源都在**外面** |
| 2 | **使用者不是被建模的对象**——它只有权限与审计记录（§5.3） |
| 3 | **数据来源也不是对象**——只有经**映射**进来的那部分才成为对象与事实（§5.3） |

**工具不与本体空间并列**——它是一条指向本体空间的箭头：只负责"建、改、管、发布"，
既不下连数据来源，也不经过使用者。

> **⚠️ 这张图是架构的唯一表述。** v0.3 曾另有一套 "L1~L6 六层架构"，与它重复且无公开出处，
> **v0.4 已删除**（理由见附录 B）。

### 0.5 【关键】形态选择：中央式权威及其代价

**本框架采用中央式：一套权威语义，所有系统向它对齐。**

**它给你什么：**

| 收益 | 说明 |
|---|---|
| **强一致** | 一处改，处处生效；不存在"你的客户"和"我的客户" |
| **无歧义** | 只有一个"营收"的定义 |
| **可治理** | 有明确的权责主体，能追责 |
| **可推理** | 全局闭包可算，不存在跨域的语义断层 |

**它要你放弃什么——必须说清，这是本框架最重要的诚实：**

| 放弃的 | 说明 |
|---|---|
| ❌ **"任何人都能改核心"** | 不可能。改核心要提案、要评审、要公示 |
| ❌ **域之间的完全自治** | 各域必须向核心对齐，不能各说各话 |
| ⚠️ **中央成为瓶颈** | 核心团队的能力决定全局；核心改动要排队 |
| ⚠️ **中央成为风险点** | 核心定义出错，影响所有消费者 |

**所以本框架的"开放"具体指什么：**

| ✅ 开放的 | ❌ 不开放的 |
|---|---|
| 规范本身（开源、任何人可自由实现） | **核心定义的直接修改权** |
| 提案权（任何人可提变更提案） | |
| 扩展命名空间（域内自定义，不受核心约束） | |
| **分叉权**（可 fork 核心，另立门户） | |
| **退出权**（可导出全部定义与事实并离开） | |

> **一句话：开放的是「加入、提案、分叉、退出」，不是「就地修改」。**

**这不是妥协。这是所有成功标准的共同形态：**

| 标准 | 提案 | 权威 |
|---|---|---|
| IETF | 任何人可提 Internet-Draft | 集中（IESG/IAB 批准） |
| W3C | 任何人可提 | 集中（工作组 + 会员投票） |
| Kubernetes | 任何人可提 KEP | 集中（SIG + TOC 批准） |
| **本框架** | 任何人可提变更提案 | **集中（治理机构批准）** |

> **"提案开放 + 权威集中"是唯一被验证过能同时做到"开放"和"稳定"的形态。**
>
> 而那些试图做到"谁都能改"的尝试（语义网的全球共享本体），**全部失败了**。
"""

NEW_P2_HEAD = r"""# 第二部分 · 五要素　★主轴

> **本部分是框架的主轴。** 它回答"语义由什么构成"。
> 契约（第三部分）回答"谁能读写"，架构图（§0.4）回答"东西放在哪"——**三者不重复**。
"""

NEW_S5 = r"""## 5. 四个正交维度：别把它们混成一锅

> **这一节是全文最容易读错的地方，所以单独拿出来先讲清。**
>
> 框架里有四套名词，**它们不是四套分类，是四个不同的问题**。
> 混在一起看，就会出现"为什么动作既是要素又是契约""为什么还要分层"这类困惑。

### 5.1 四个维度各答一个问题

| 维度 | 回答什么 | 出处 | 性质 |
|---|---|---|---|
| **五要素** | 语义**由什么构成**（内容分类） | 对象·关系·规则 ← **Apache Ossie**<br>策略·动作 ← **本框架原创** | 声明侧 |
| **TBox / ABox** | 这句话在讲**类型还是事实**（角色） | **描述逻辑**（学术理论，OWL 的基础区分） | 角色 |
| **Apache Ossie** | 怎么写进文件（**格式**） | **Apache 标准** | 格式 |
| **契约** | 谁**能**读写、怎么读写（**通道**） | 本框架原创 | 通道 |

**读法**：**一个东西在四个维度上各有一个坐标，四个坐标互不替代。**

```
「订单」这个东西：
  · 五要素轴：它是【对象】
  · TBox/ABox 轴：作为定义它在【TBox】；作为 #12345 那条记录它在【ABox】
  · 格式轴：它写成 【Ossie 的 concept: Order】
  · 契约轴：谁读它走【I1】，谁写它走【I2】
```

### 5.2 TBox / ABox 是「角色」，不是「层」

> **⚠️ 这是 v0.4 的一处重要更正。**
> v0.3 曾写"L3 语义层 = TBox 的载体、L2 事实层 = ABox 的载体"——
> **"载体"这个词把它说成了两个房间**，于是凭空多出一套"层"，与 §0.4 的架构图冲突。

**正确的说法**：**TBox 与 ABox 是同一个东西的两种角色。**

| 要素 | TBox 角色（定义：允许什么） | ABox 角色（事实：实际是什么） |
|---|---|---|
| **对象** | 「订单」**这个类型**存在 | 「订单 #12345」**这个实例** |
| **关系** | 「订单—属于→客户」**这个类型化连接** | 「#12345 属于 acme」**这条事实** |
| **规则** | 「金额必须为正」**这条约束** | —— 规则**没有事实侧**（只在入口判定） |
| **策略** | 「优先走低时延」**这条偏好** | —— 策略**也没有事实侧** |
| **动作** | 「取消订单」**这个契约** | 一次提交**及其审计记录** |
| **派生** | `derived_by` **表达式** | 派生结果（**不落库**，现算现用） |

**对应到 §0.4 的架构图：**

| 图里的位置 | 角色 |
|---|---|
| **本体空间** | 装着 **TBox**（五个要素的声明） |
| **数据来源** | 提供 **ABox**（真实数据） |
| **映射 + 契约** | 让两者对上（字段 → 对象与事实） |

> **所以"TBox/ABox 要分开"不是一条"层的规则"，是一条"纪律"**——
> 定义与事实必须分属不同的存放单元（`LD-4`）。
> 这件事在架构图里已经表达了：本体空间是定义侧，数据来源是事实侧。

### 5.3 本体空间之外的两样东西

**这两样常被误当成要素或对象，必须说清：**

| | 在不在本体空间里 | 它是什么 |
|---|---|---|
| **数据来源** | **不在** | 数据从哪来。只有**经映射进来的那部分**才成为对象与事实 |
| **使用者**（人 / 智能体） | **不在** | 契约的调用方。它没有标识、能力、状态——**只有权限与审计记录** |

**为什么必须分清**：

- **数据来源不清** ⇒ 没人知道"数据在哪一刻、按哪份声明变成对象"，"写入必须过规则"就没有落脚点
- **使用者不清** ⇒ 智能体被塞进对象清单，而它根本没有"标识、能力、状态"这三样对象属性

**映射是声明的，不是隐含的**：归 **I5 接入契约**，用 Ossie 的 `concept_mappings` /
`object_mappings` / `link_mappings` 声明"哪个字段变成哪个对象的哪个属性"。

### 5.4 五个要素一览（**含出处**）

> **"可声明"= 可以写进契约文件的东西。** 其余（实例、事实）是运行时的。

| # | 要素 | 声明什么 | **出处** | 载体 |
|---|---|---|---|---|
| 1 | **对象** | 有哪些类东西、层次、标识 | ✅ **Apache Ossie** | `concept` `type` `extends` `identify_by` |
| 2 | **关系** | 对象之间怎么连（角色、基数、方向） | ✅ **Apache Ossie** | `relationships` `roles` `multiplicity` `verbalizes` |
| 3 | **规则** | 什么合法（**约束式** · **派生式**） | ⚠️ **槽位来自 Ossie**，表达式语法 Ossie 未定 | `requires` `derived_by` |
| 4 | **策略** | 在合法候选里选哪个（序，可权衡） | ❌ **无公开规范 → 本框架原创**；语义参照 OMG DMN / W3C ODRL | 旁挂声明（§9.7） |
| 5 | **动作** | 允许怎么改、带什么条件、连带动什么 | ❌ **无公开规范 → 本框架原创**；概念参照 Palantir kinetic elements | 旁挂声明（§10.9） |

**前两个是名词性的**（世界上存在什么），**后三个是动词性的**（允许做什么、应该怎么做）。

> **Ossie 覆盖前三个的槽位；策略与动作是空白——这两个是本框架的原创部分。**
> 这个判断有据可查，不是拍脑袋：**Ossie 本体规范里根本没有"策略"或"动作"这两个概念**，
> 也**没有任何扩展机制**（`ontology.md` 里 extension/custom/vendor 出现 0 次）。

### 5.5 三句话把五要素的关系定死

| 要素 | 一句话 | 失败算什么 |
|---|---|---|
| **规则** | 划定可行域的**边界** | 系统**出错**（出现非法状态） |
| **策略** | 在可行域内建立**偏好序** | 系统**次优**（仍然合法，只是效果差） |
| **动作** | 把选择**落地**为状态变更 | 提交被拒，或需要补偿 |

**派生不是独立要素**：声明归规则（`derived_by` 与 `requires` 是同一要素的两种声明形式），
产物归对象（**派生对象**），且**不落库**（`I2-4`）。

**三条切割线**（判断一个声明属于哪个要素，展开见 §8.3 / §9.3）：

| 切割线 | 规则 | 策略 | 动作 |
|---|---|---|---|
| **论域** | 尚未受约束的状态空间 | 规则筛出的**合法候选集** | 已选定的那一个 |
| **值域** | **二值**（合法 / 非法） | **序**（优劣与选中项） | **状态变更** |
| **失败语义** | 系统**出错** | 系统**次优** | 提交被拒 / 需要补偿 |

**一条最好用的现场判据——拿掉它看后果：**

| 拿掉它 | 归属 |
|---|---|
| 产生**非法状态** | **规则** |
| 系统**仍然合法，只是效果变差** | **策略** |
| **没有任何状态改变** | **不是规则也不是策略**——它根本不在这里 |

**由三条切割线推出的两条硬结论**：

1. **规则与策略 MUST NOT 产生状态变更；凡产生状态变更者必属动作。**（`ELE-8`）
2. **策略的论域是规则论域的子集**——所以"把优选塞进规则"与"把合法性交给策略"都是语义污染。

### 5.6 搭框架的准则：按公开出处来

> **有槽就用槽，没槽就用别的公开标准，都没有才自定——且必须标明。**

| 顺序 | 判断 | 处置 |
|---|---|---|
| 1 | Ossie 有对应槽位吗？ | **有 → 直接用**（对象、关系、规则的槽位） |
| 2 | 没有——别的公开标准有吗？ | **有 → 照标准做并标明**（策略 → DMN / ODRL） |
| 3 | 都没有——必须自己定吗？ | **是 → 自定，但 MUST 标明"本框架原创，无公开规范可依"** |

**这条准则的价值在第 3 档：一标注就露馅。**
凡是说不出来源的，就不该以"标准"或"推导"的面目出现——
v0.3 的"六层架构"正是这么被发现的（它其实是把 Palantir 的后端服务反推成层，再补的理论说辞，见附录 B）。
"""

NEW_S11 = r"""## 11. 纪律条款

> 这一组是**跨要素**的纪律——不描述单个要素，而约束它们**怎么放在一起**。
> 编号 `LD-n`（LD = Layer Discipline → 现改称"纪律"）。全部可检验。

### 11.1 接入侧是条件能力

**事实进入系统有两个入口，它们不是同一件事：**

| 入口 | 触发者 | 负责什么 | 典型场景 |
|---|---|---|---|
| **接入侧** | **外部系统** | 把**外部表示**翻译成合法事实 | 外部数据源、ERP/CRM/数仓、消息流、设备 |
| **动作** | 人 / 智能体 / 内部逻辑 | 在**内部**产生或修改事实 | 用户在界面上提交一次修改 |

| 编号 | 条款 |
|---|---|
| **LD-1** | 接入侧 **MUST** 被视为**条件性能力**：只在存在外部数据源时出现。**MUST NOT** 假定它必然存在 |
| **LD-2** | 接入侧缺席时，进入事实的路径**只能**是动作——此时"只有动作能写"从一条约束升级为**唯一路径** |
| **LD-3** | 实现 **MUST** 显式声明接入侧存在与否，**MUST NOT** 用"这部分没有内容"暗示 |

**LD-2 是个意外的收获**：接入侧缺席反而让「只有动作能写」变得**更强、更可检验**——
有接入侧时，你要证明"外面来的一定过了规则"；没有时，**写入路径只有一条，堵住它就堵住了全部**。

> **缺席不是缺陷，是域的属性。** 但它有代价，MUST 被记下来而不是等要用时才发现：
> 整块缺席 ⇒ 没有任何"把外部表示翻译成合法事实"的能力；
> 想接入真实数据源时，要**新做一整块**，而不是加一个适配器。

### 11.2 定义与事实 MUST 分属不同的存放单元

> §5.2 说 TBox 与 ABox 是两种**角色**。这一条把角色落成一条**可检验的纪律**。

若同一个单元既是"允许什么"又是"实际是什么"的载体，后果是可枚举的：

| 后果 | 说明 |
|---|---|
| 改定义要动数据 | **低频变更被高频数据绑住** |
| 同一份事实无法换定义投影 | 而"多投影"正是分层的理由 |
| 任何"要素 × 角色"的表都画不出来 | 只能把它塞进一格，**表格因此不准** |

| 编号 | 条款 |
|---|---|
| **LD-4** | 定义（TBox 角色）与事实（ABox 角色）**MUST NOT** 混装在同一个存放单元内；混装 **MUST** 被登记为已知偏差 |
| **LD-5** | 若因域的特性（如事实为构建期常量、与定义同生命周期）暂不拆分，**MUST** 写明**触发拆分的条件**（例：出现运行期可变事实） |

**LD-5 的意义**：不拆是可以的，但**"为什么不拆"必须是一个有条件的判断**，
而不是"一直没拆"。条件一旦成立（域变动态），拆分就从"可选优化"变成"必须做的事"。

### 11.3 契约只声明五要素，不得增设第六个

| 编号 | 条款 |
|---|---|
| **LD-6** | 契约 **MUST** 按五要素声明；**MUST NOT** 增设第六个要素（同 `ELE-1`） |
| **LD-7** | 接入侧、数据来源、使用者 **MUST NOT** 被当作要素——它们是要素**之外**的东西（§5.3） |

**为什么必须写死**：一旦允许增设，就会出现"规则产出要素""策略也是约束"这类跨要素的生产关系，
**要素之间的边界随之失效**——而边界一旦失效，三条切割线就全塌了。

### 11.4 【附】实现别名 L1~L6（**不是架构分层**）

> **⚠️ 先读这句**：下面这张表**不是**一套架构分层，而是本文档为**指代"某个处理环节"**而用的**简写别名**。
>
> **架构的唯一表述是 §0.4 的图。** 这里只是为了后文能简洁地说"谁读谁写"。
>
> v0.3 曾把它写成"六层架构"并配一张大图，**v0.4 已删除**——因为它没有公开出处
> （是把 Palantir 的后端服务反推成层），且与 §0.4 重复（详见附录 B.1）。

| 别名 | 指代的处理环节 | 对应 §0.4 的什么 |
|---|---|---|
| **L1 接入** | 把外部表示变成合法事实的环节（= **接入侧**，条件性能力，`LD-1`） | 数据来源 → 本体空间 的**入口** |
| **L2 事实** | 实例与事实的存放 | 数据来源侧（**ABox 角色**） |
| **L3 语义** | 五个要素的声明（TBox 角色的载体） | **本体空间** |
| **L4 查询** | 读取、遍历、派生求值 | 使用者的**读路径** |
| **L5 动作** | 受控修改的执行环节 | 使用者的**写路径** |
| **L6 消费** | 契约的调用方（人 / 智能体） | §0.4 图的**使用者**（在外） |

**两条使用纪律：**

| 编号 | 条款 |
|---|---|
| **LD-8** | 文档与实现 **MUST NOT** 把 L1~L6 当作"架构分层"来理解或宣传；它只是**处理环节的简写** |
| **LD-9** | 与外部对齐时 **MUST** 优先使用五要素与契约的措辞（`§5` / `§14`），L1~L6 仅用于内部简短指代 |
"""

QUICK_NEW = r"""---

# 附录

---

## 附录 A · 一页速查

```
【理念】语义治理 = 权责 + 契约 + 技术

【形态】中央式权威
  开放：加入 · 提案 · 分叉 · 退出
  不开放：核心定义的直接修改权
  （"提案开放 + 权威集中"是唯一被验证过的形态）

【治理六件事】
  命名空间与权威 · 变更流程 · 争议解决 · 版本兼容 · 分叉退出 · 权责矩阵

【三种剖面】
  A 轻量（记忆库）· B 组织（部门治理）· C 严格（跨系统权威）

【四个正交维度】★ 别混
  五要素（内容）· TBox/ABox（角色）· Ossie（格式）· 契约（通道）

【五要素】★主轴   —— 逐个都标出处
  对象 ✅ Ossie    concept / type / extends
  关系 ✅ Ossie    relationships / roles / multiplicity
  规则 ⚠️ 槽位来自 Ossie（requires / derived_by），表达式语法 Ossie 未定
  策略 ❌ 无公开规范 → 本框架原创（语义参照 DMN / ODRL）
  动作 ❌ 无公开规范 → 本框架原创（概念参照 Palantir kinetic elements）
  （派生不是要素：声明归规则，产物归对象，不落库）

【搭框架的准则】
  有槽就用槽 → 没槽就用别的公开标准 → 都没有才自定，且 MUST 标明出处

【架构】见 §0.4
  使用者（在外）→ 本体空间（五要素）→ 数据来源（在外）
  本体构建与管理工具 ──指向──▶ 本体空间

【六个契约】★框架核心
  I1 语义  ← Ossie ✅
  I2 事实  ← GQL + 【补：写入必须过规则】
  I3 查询  ← MCP + 【补：结果必须可追溯】
  I4 动作  ← ❌ 原创（抽象模型）
  I5 接入  ← ODCS ✅
  I6 发现  ← ❌ 原创（能力目录）

  要发明两个（I4、I6）；要补两条约束（I2-1、I3-2）；其余是选型组合。

【五级合规】
  L0 可声明 → L1 可读 → 【分水岭】→ L2 可写 → L3 可治理 → L4 可运营

【纪律条款】
  LD-1~LD-3 接入侧是条件能力（缺席时"只有动作能写"成为唯一路径）
  LD-4~LD-5 定义与事实分属不同存放单元；不拆要写明触发条件
  LD-6~LD-7 契约只声明五要素，不得增设第六个；接入/来源/使用者不是要素

【Agent-first 四原则】
  一切可枚举 · 一切可溯源 · 拒绝即可自纠 · 一切有预算

【最便宜的高价值动作】
  建"该被拒绝的样本集"，每次改规则跑一遍

【开放问题】9 条未解决，其中第 8、9 条是对框架本身的质疑
```

---

## 附录 B · 从 v0.3 到 v0.4 的迁移对照

> 本附录记录**删了什么、为什么删**——避免以后有人重新把删掉的东西加回来。

### B.1 删掉了「L1~L6 六层架构」

| | |
|---|---|
| **删掉的是什么** | L1 接入 · L2 事实 · L3 语义 · L4 查询 · L5 动作 · L6 消费，以及"三条流" |
| **它原本从哪来** | **把 Palantir 的后端服务反推成层**：Funnel → L1、Object Databases → L2、OMS → L3、OSS → L4、Actions service → L5。之后再补上"从 TBox/ABox 推导"的说法 |
| **为什么删** | ① **没有公开出处**——Ossie 规范里 `layer` 出现 0 次，Palantir 官方分的是 Language / Engine / Toolchain 三层<br>② **与 §0.4 架构图重复**——图里已经说清了"东西放在哪"<br>③ **制造三轴混淆**——它让读者以为系统要按层搭，与"五要素是主轴"打架 |
| **其中有没有该留的** | 有两条**纪律**（原本叫 `L-5`~`L-9`），已改写成 §11 的 `LD-1`~`LD-5`：<br>· 接入侧是条件能力（原 L-5~L-7）<br>· 定义与事实分属不同存放单元（原 L-8~L-9） |

### B.2 TBox / ABox 从「层」改成「角色」

| v0.3 的说法 | v0.4 的说法 |
|---|---|
| "L3 语义层 = TBox 的载体" | **TBox 是一种角色**（这句话在讲类型），不占架构位置 |
| "L2 事实层 = ABox 的载体" | **ABox 是一种角色**（这句话在讲事实） |

**改动理由**："载体"这个词把角色说成了**房间**。正确的表述见 §5.2 的角色对照表。

### B.3 要素称谓与条款编号的调整

| v0.3 | v0.4 |
|---|---|
| 「接口」 | 「**契约**」——"接口"暗示分层，而契约的要点是"谁能读写" |
| 要素条款 `E-1`~`E-8` | `ELE-1`~`ELE-8`（避开 I4 规范的 `E-` 表达式语言条款） |
| Agent-first 条款 `A-1`~`A-5` | `AG-1`~`AG-5`（避开《模块纪律》与 I4 的 `A-` 条款） |
| `L-5`~`L-9`（分层条款） | `LD-1`~`LD-5`（纪律条款，见 §11） |
| 正文用「概念」 | 正文一律用「**对象**」；`concept` 只作 Ossie 载体名 |

### B.4 哪些没变

- **§14~§28 的编号全部未变**（契约 · 模块 · 合规 · 规范条款 · 实施 · 开放问题）——全仓引用不断
- **§4 术语表 · §6~§10 五要素 · §1~§3 治理维度**：逐字保留
- **六个契约本身**（I1~I6 及其条款 `I1-1`…`I6-3`）：未变
"""

CONTENT = dict(NEW_HEAD=NEW_HEAD, NEW_S0=NEW_S0, NEW_P2_HEAD=NEW_P2_HEAD,
               NEW_S5=NEW_S5, NEW_S11=NEW_S11, QUICK_NEW=QUICK_NEW)
