"""一次性：修最后一批一致性问题（审阅报告 1.1 / 5.1 / 6.3 / 8.x / C / D / I / M / N / K）。"""
from pathlib import Path

ROOT = Path(r'D:\Code\04-research-ontology\semantic-governance')
total = 0


def fix(path, pairs, label):
    global total
    p = ROOT / path
    t = p.read_text(encoding='utf-8')
    n = 0
    for old, new in pairs:
        if old in t:
            t = t.replace(old, new); n += t.count(new) and old in t and 1 or 1
        else:
            print(f'    ⚠ 未命中: {old[:58]}')
    if n:
        p.write_text(t, encoding='utf-8', newline='\n')
    print(f'  {label}: 处理 {len(pairs)} 条')
    total += len(pairs)


fix('docs/framework.md', [
    # 1.1 写入权两处仍写"仅 L5"
    ('| **实例与事实** | —（运行期产生） | **I2** · I3 · I6 | **I2**（且仅 L5 能写） |',
     '| **实例与事实** | —（运行期产生） | **I2** · I3 · I6 | **I2**，且只有两条路径（接入 / 动作，`I2-G3`） |'),
    ('| **I2 事实** | **GQL**（ISO/IEC 39075:2024） | ⭐ **写入必须过规则**；只有 L5 能写 |',
     '| **I2 事实** | **GQL**（ISO/IEC 39075:2024） | ⭐ **写入必须过规则**；只有两条写入路径（`I2-G3`） |'),
    # C. I1-2 误用
    ('对象（定义）**一经发布不可原地修改**，只能新增版本（`I1-2`、`G-2`）。',
     '对象（定义）**一经发布不可原地修改**，只能新增版本（`I1-G1`、`G-2`）。'),
    # N. 九项 → 十项
    ('### 10.2 动作契约九项', '### 10.2 动作契约字段清单（十项）'),
    ('| **最小字段**（对应 §10.2 的契约九项）', '| **最小字段**（对应 §10.2 的字段清单）'),
    # 3.1 幂等：副作用 → 效果（与 i4 ID-2 一致；最小 I4 里 side_effects 必须为空）
    ('| **I4-G5** | **幂等** | **MUST** 支持幂等键；重放 **MUST** 返回原结果，**MUST NOT** 重复产生副作用 |',
     '| **I4-G5** | **幂等** | **MUST** 支持幂等键；重放 **MUST** 返回原结果，**MUST NOT** 重复执行**效果** |'),
    # 5.1 AG-2 与 I4-8 的层级说清（MUST 的是"可自纠"，替代动作是 SHOULD）
    ('| AG-2 | 所有拒绝 **MUST** 可被自动纠正（含足够信息让调用方重试成功） |',
     '| AG-2 | 所有拒绝 **MUST** 可被自动纠正（含足够信息让调用方重试成功）——**注意**：要求的是"信息足以自纠"，其中**替代动作建议是 SHOULD**（`I4-8`），实现 **MUST NOT** 依赖它存在 |'),
    # 8.5 L0/L1 不得声明策略 → 归入规范条款
    ('| **SP-7** | **只读实现（无 I4）MUST NOT 声明策略**——它没有落地的能力 | §9.9 |',
     '| **SP-7** | **只读实现（无 I4）MUST NOT 声明策略**——它没有落地的能力（L0/L1 即此类） | §9.9 |'),
    # I. 附录 A 重复行（LD-4~5 / LD-6~7 各出现两次）
    ('''  LD-4~LD-5 定义与事实分属不同存放单元；不拆要写明触发条件
  LD-6~LD-7 契约只声明五要素，不得增设第六个；接入/来源/使用者不是要素
  LD-8~LD-9 L1~L6 只是处理环节的简写，不得当作架构分层理解或宣传
  LD-4~LD-5 定义与事实分属不同存放单元；不拆要写明触发条件
  LD-6~LD-7 契约只声明五要素，不得增设第六个；接入/来源/使用者不是要素''',
     '''  LD-4~LD-5 定义与事实分属不同存放单元；不拆要写明触发条件
  LD-6~LD-7 契约只声明五要素，不得增设第六个；接入/来源/使用者不是要素
  LD-8~LD-9 L1~L6 只是处理环节的简写，不得当作架构分层理解或宣传'''),
], 'framework')

# C 的跨文件部分：module-discipline 也把 I1-2 当"定义不可变"
fix('spec/module-discipline.md', [
    ('框架 `I1-2` 要求"定义不可变"', '框架 `I1-G1` 要求"定义不可变"'),
    ('| `I1-2` 定义不可变 |', '| 框架 `I1-G1` 定义不可变 |'),
    ('| §17 模块视图 |', '| §22 模块视图 |'),
], 'module-discipline')

# H：i3 的 R-5 不存在（框架只有 R1~R4）
fix('spec/i3-query-minimal.md', [
    ('（`R-5` 同理：可重现）', '（同 `R1`：快照一致，可重现）'),
], 'i3')

print(f'\n完成')
