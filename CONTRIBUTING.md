# 参与贡献

> **本框架要求所有使用它的组织实行"提案制"变更流程。
> 所以这个仓库自己必须先做到。**

---

## 一、基本原则

| 原则 | 说明 |
|---|---|
| **提案开放** | 任何人（不限身份、不限组织）可以提变更提案 |
| **权威集中** | 提案的批准权集中在维护者 |
| **分叉自由** | 不满意就直接 fork，Apache-2.0 允许任何用途 |
| **退出自由** | 仓库内容随时可完整导出 |

> **开放的是「加入、提案、分叉、退出」，不是「就地修改」。**

---

## 二、你可以做什么

### 2.1 提 Issue（最轻）

不需要模板。任何以下内容都欢迎：

- **反驳** —— 这个设计哪里错了
- **场景** —— 你打算用它做什么，哪一层不适用
- **问题** —— 看不懂、有歧义、缺定义
- **实现反馈** —— 你实现到哪一级了，卡在哪

### 2.2 提变更提案（正式）

用于**修改框架正文**（`docs/framework.md`）。

流程：

```
① 复制 proposals/0000-template.md 为 proposals/NNNN-简短标题.md
       ↓
② 填写提案（背景、变更内容、影响分析、兼容性）
       ↓
③ 提 Pull Request，标题为 "RFC: <标题>"
       ↓
④ 公示期 ≥ 7 天（破坏性变更 ≥ 14 天）
       ↓
⑤ 维护者评审
       ↓
⑥ 批准则合并，正文更新，版本号递增
```

### 2.3 提实现

本框架目前**没有参考实现**。这是最需要的工作。

如果你实现了某一级合规，欢迎：

- 在 Issue 里说明你实现了哪一级、覆盖哪些条款
- 提交一个 `implementations/` 条目（格式待定，见 Issue）

---

## 三、变更的硬规则

以下规则来自框架正文 §20.1，**对本仓库自身同样适用**：

| # | 规则 |
|---|---|
| **G-1** | 每个定义项 **MUST** 属于某个命名空间 |
| **G-2** | 定义一经发布 **MUST NOT** 被原地修改，变更 **MUST** 产生新版本 |
| **G-3** | 改变语义时 **MUST** 新建条款，**MUST NOT** 复用旧名 |
| **G-4** | 破坏性变更 **MUST** 提供弃用期 |
| **G-6** | 变更 **MUST** 可计算影响面 |

**特别地，G-3 的直接含义：**

> 如果一个条款的含义变了，**不要改它，新增一条并弃用旧的**。
> "改语义但保留名字"是最危险的变更——它让所有读者静默出错。

---

## 四、版本与弃用

| 变更类型 | 版本 | 弃用期 |
|---|---|---|
| 澄清表述、修错别字 | patch | 无 |
| 新增条款、新增接口 | minor | 无 |
| 修改 MUST 条款 | **major** | ≥ 14 天 |
| 删除条款、删除接口 | **major** | ≥ 30 天 |

**弃用写法：**

```markdown
> ⚠️ **DEPRECATED（自 v0.3 起，将于 v0.5 移除）**
> 原条款内容……
> 替代：见 §X.Y
> 弃用理由：……
```

---

## 五、当前阶段说明

**仓库处于 RFC（征求意见）阶段，v0.x。**

此阶段的特殊性：

| 事项 | 说明 |
|---|---|
| **所有内容都是"核心层"** | 因为还没有发布稳定版，没有扩展层 |
| **可以有破坏性变更** | 但必须在提案里写明 |
| **公示期从 7 天起** | 稳定后可能延长 |
| **维护者** | 现阶段为仓库所有者；稳定后引入治理机构 |

**发布 v1.0 的条件（暂定）：**

- [ ] 至少一个完整实现达到 Level 2
- [ ] I4 动作接口有可实现的详细规范
- [ ] 一致性测试套件可用
- [ ] 至少两个独立场景验证过（如：Agent 记忆库 + 部门治理）

---

## 六、文风约定

- **区分 Normative 与 Informative**：规范条款用 MUST / MUST NOT / SHOULD；实施建议不加这些词
- **术语必须与 `docs/framework.md` §4 术语表一致**，不得自己造词
- **中文为主**，关键术语给出英文
- **不写没有出处的数字**（如"工作量占比 40%"这种，除非有依据）

---

## 七、行为准则

- 讨论**设计**，不攻击**人**
- 可以直说"这个设计错了"，但要说清**为什么错**
- **分叉是被允许且被鼓励的** —— 如果你觉得方向不对，fork 走比争论有效

---

## 八、许可

所有贡献均以 [Apache License 2.0](LICENSE) 授权。
