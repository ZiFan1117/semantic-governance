# I3 查询接口 · 最小规范

| 字段 | 值 |
|---|---|
| **版本** | v0.2 |
| **规范等级** | **Normative** |
| **上级文档** | [`../docs/framework.md`](../docs/framework.md) §12 |
| **前置** | 本规范首次实现 Ossie 的 `derived_by`（框架 §5 中标注为"Ossie 覆盖但参考实现未支持"） |

---

## 0. 范围

### 0.1 在范围内

| # | 内容 |
|---|---|
| 1 | 事实的模式查询 |
| 2 | 多跳遍历 |
| 3 | **派生事实的求值（`derived_by`）与溯源** |
| 4 | 简单检索 |
| 5 | 预算控制与**显式截断** |

### 0.2 不在范围内

| 排除项 | 说明 |
|---|---|
| 向量语义检索 | 属检索增强，最小版只做字面匹配 |
| 聚合与统计 | 属分析，不属本接口 |
| 查询语言的标准语法 | 最小版用结构化参数，不定义查询 DSL |
| 派生规则的完整表达力 | 见 §3.3，只支持路径型规则 |

---

## 1. 接口

| 操作 | 输入 | 输出 |
|---|---|---|
| `Query` | 模式（subject / relation / object 的任意组合） | 事实列表 |
| `Traverse` | 起点 + 关系路径 + 深度 | 可达实例与路径 |
| `Infer` | 派生目标 | **含派生的**事实列表（每条带 `via`） |
| `Search` | 文本 | 命中的事实 |

---

## 2. 通用约定

### 2.1 结果信封

**所有操作的返回 MUST 使用统一信封：**

```json
{
  "items": [ ... ],
  "truncated": false,
  "snapshot": 5,
  "budget": { "max_items": 100, "returned": 12 },
  "bypass_permission_filter": false
}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `items` | ✅ | 结果项 |
| `truncated` | ✅ | **是否因预算被截断** |
| `snapshot` | ✅ | 本次查询所依据的快照（`R-5` 同理：可重现） |
| `budget` | ❌ | 预算与实际返回量 |

### 2.2 三条硬条款

| # | 条款 | 理由 |
|---|---|---|
| **Q-1** | **截断 MUST 显式**（`truncated: true`），**MUST NOT** 静默返回部分结果 | Agent 会把不完整当完整，据此做错决策 |
| **Q-2** | 同一次查询的**所有读取 MUST 基于同一快照** | 否则结果自相矛盾 |
| **Q-3** | 结果顺序 **MUST** 确定 | 否则无法缓存、无法复现 |

---

## 3. 派生事实

### 3.1 语义

Ossie 的 `derived_by` 定义"由其他事实推出的事实"。
这些事实**不落库**（框架 `I2-4`），**在查询时求值**。

### 3.2 溯源（**本规范的核心**）

> **每条派生事实 MUST 携带 `via`，说明它是被哪条规则、从哪几条事实推出的。**

```json
{
  "subject": "urn:...:Person:alice",
  "relation": "ancestor_of",
  "object": "urn:...:Person:carol",
  "derived": true,
  "via": {
    "rule": "Person.ancestor_of",
    "case": 1,
    "expression": "Person.ancestor_of.parent_of(descendant)",
    "premises": [
      { "subject": "urn:...:Person:alice",
        "relation": "ancestor_of", "object": "urn:...:Person:bob" },
      { "subject": "urn:...:Person:bob",
        "relation": "parent_of", "object": "urn:...:Person:carol" }
    ]
  }
}
```

| 字段 | 说明 |
|---|---|
| `derived` | 恒为 `true`（用于区分存储的事实与推出的事实） |
| `via.rule` | 哪条派生定义（<概念>.<关系>） |
| `via.case` | 该定义的第几条 `derived_by` 表达式（**从 0 计**） |
| `via.expression` | 该表达式原文 —— **让调用方不必回查定义** |
| `via.premises` | 所依据的事实（最小支持 1~2 条） |

> **没有 `via` 的派生事实是"无理由的断言"。**
> Agent 拿着它无法回答"凭什么"，也就无法验证 —— 这正是框架 §20.8 `A-2` 的要求。

### 3.2.1 两种规范未覆盖的派生形态（`V-1` / `V-2`）

> **本节来自实现。** §3.2 的模型默认「**一条事实一条 `via`**」，
> 且默认「派生**总有**前提」。实际写起来这两种默认都不成立。

#### `V-1` 集合值派生：一条规则推出一组事实

"喜用神 = {土, 火}" 是**一条规则**（第 0 条 `favorable_rules` 的 `restricts + generates`）的产物，
**不是**两条独立规则各推一个元素。

| 编号 | 级别 | 条款 |
|---|---|---|
| **V-1** | MUST | 当一条规则推出**多个**事实时，实现 **MUST** 明确表达这份 `via` 覆盖哪些成员（如记下 `members`），**MUST NOT** 靠"逐成员复制同一份 `via`"暗示每个成员各有理由 |

逐成员复制同一份 `via` 的害处不是浪费，而是**误导**：
读的人会以为"土"和"火"各有独立的推导过程，于是去找那条并不存在的第二条规则。

#### `V-2` 否定性派生：从"没有匹配"得出结论

有些派生是**从缺席得出的**：身弱 = "没命中任何旺规则"。此时诚实的依据集合是**空的**，
而 §3.2 又要求 `premises` 非空。三条路：

| 做法 | 问题 |
|---|---|
| 伪造一条正向前提（如 `木 not_same_as 金`） | **撒谎**：存储里没有这条事实，`via` 也就无法被验证 |
| 允许 `premises: []` | 与"无理由的断言"直接冲突，且**无法与漏填区分** |
| **显式表达否定事实** | 说明"我对这组规则求值过，没有命中"——一条真的、可复核的断言 |

| 编号 | 级别 | 条款 |
|---|---|---|
| **V-2** | MUST | 当派生来自"未命中任何规则"时，`via` **MUST** 显式表达这一**否定事实**，**MUST NOT** 伪造正向前提，**MUST NOT** 留空 `premises` |
| **V-2a** | MUST | 否定事实 **MUST** 用一个**保留谓词**表示（参考实现用 `no_match_in`），且校验器 **MUST** 据此**跳过**存储核对（它本来就不该在存储里），但仍 **MUST** 要求它存在 |

> **对验收样本 2 的影响**：样本 2 说"`via.premises` 里的每条前提必须在存储中真实存在"
> —— 加上 `V-2a` 之后，这条应读作"**每条非否定前提**"。
> 校验器不能对未知谓词放行（那是漏洞），只能对**显式声明为否定**的谓词放行。

### 3.3 最小表达力（**明确边界**）

> **表达式语言归属 Ossie**（`core-spec/expression_language.md`，SQL 子集）。
> **本规范只约束"最小实现必须支持到什么程度"，不另行定义语言。**
> 见 [`i4-action-minimal.md`](i4-action-minimal.md) §2.3。

**支持的派生表达式形式：路径型**

```
<概念>.<关系1>[.<关系2>...](<角色名>)
```

| 情形 | 形式 | 例 |
|---|---|---|
| **基础** | `C.r(role)` | `Person.parent_of(descendant)` |
| **递归** | `C.<自身>.r(role)` | `Person.ancestor_of.parent_of(descendant)` |
| **泛化路径** | `C.r1.r2(role)` | `Order.shipment.carrier(role)` |

**不支持（MUST 明确拒绝，不得静默忽略）：**

| 不支持的形式 | 例子 |
|---|---|
| 布尔组合 | `A.files_single AND A.earns <= 11925` |
| 比较与算术 | `Amount > 0.0` |
| 否定 | `NOT EXISTS(...)` |
| 聚合 | `COUNT(...)` |

> **遇到不支持的表达式，实现 MUST 报错或标注"未支持"，MUST NOT 当作空结果。**

### 3.4 求值算法：不动点

```
重复：
    对每条派生定义、每条 derived_by 表达式：
        扫描当前事实集，尝试匹配
        若产生新事实 → 加入工作集，记录 via
直到一轮下来没有新事实
```

**这是框架 §14 描述的"前向链到不动点"的最小实现。**

**条款：**

| # | 条款 |
|---|---|
| **D-1** | 派生 **MUST** 求值到不动点 |
| **D-2** | 派生事实 **MUST NOT** 被持久化（框架 `I2-4`） |
| **D-3** | 实现 **MUST** 声明支持的表达式形式；不支持的 **MUST** 报错 |
| **D-4** | 实现 **MUST** 有迭代上限，超限 **MUST** 报错而非静默停止 |
| **D-5** | 同一事实有多条推导路径时，**MAY** 只返回其中一条，但 `via` **MUST** 真实反映该路径 |

**关于 D-4**：递归派生可能不终止（框架 §14 已指出）。
**必须给上限**，且超限要**报错**，不能悄悄停下来交出一个不完整的闭包 ——
那正好违反 `Q-1` 的立意。

---

## 4. 遍历

```
Traverse(from, path, max_depth, budget) → { items: [ { instance, path, hops } ] }
```

| 条款 | 说明 |
|---|---|
| **T-1** | **MUST** 支持多跳；深度由调用方指定 |
| **T-2** | **MUST** 检测环并终止（不得无限展开） |
| **T-3** | **MUST** 返回到达路径（便于解释"怎么连上的"） |
| **T-4** | 到达同一实例的多条路径 **MAY** 只返回最短的一条 |

---

## 5. 检索

```
Search(text, budget) → { items: [ { subject, relation, object, matched_field } ] }
```

**最小版只做字面匹配**（对字面量对象的子串匹配）。

| # | 条款 |
|---|---|
| **S-1** | 最小实现 **MUST** 至少支持字面量匹配 |
| **S-2** | 结果 **MUST** 标明命中字段 |
| **S-3** | 语义检索（向量）**SHOULD** 由实现另行提供，**MUST** 与字面匹配可区分 |

---

## 6. 规范性条款汇总

| 组 | 条款 |
|---|---|
| **通用** | Q-1 截断 MUST 显式 · Q-2 同一快照 · Q-3 顺序确定 |
| **派生** | D-1 求值到不动点 · D-2 不落库 · D-3 声明支持的表达式 · **D-4 迭代上限且超限报错** · D-5 多路径 MAY 择一 |
| **遍历** | T-1 多跳 · T-2 检测环 · T-3 返回路径 · T-4 MAY 只返最短 |
| **检索** | S-1 字面匹配 · S-2 标明命中字段 · S-3 语义检索需可区分 |
| **保证**（框架 §12） | G1 只读 · **G2 可追溯（`via`）** · G3 权限过滤 · G4 预算可限 |

---

## 7. 一致性验收

| # | 验收项 |
|---|---|
| 1 | 基础派生：给定 `a→b, b→c`，`Infer` **必须**返回 `a→c`，且 `via.case` 指向递归规则 |
| 2 | **溯源真实性**：`via.premises` 里的每条前提 **必须**在存储中真实存在（或本身是派生事实） |
| 3 | **不动点**：`a→b→c→d` **必须**推出 `a→d`（需 2 轮迭代） |
| 4 | **不落库**：`Infer` 之后，`Query` **不得**返回派生事实 |
| 5 | **截断显式**：设 `max_items=1` 而实际有 3 条，**必须** `truncated: true` |
| 6 | **不支持的表达式**：含 `AND` 的 `derived_by` **必须**报错或标注未支持 |
| 7 | **环终止**：`a→b→a` 的传递闭包 **必须**终止 |
| 8 | 权限过滤：不可见的事实 **不得**出现在结果中 |
| 9 | 快照一致：同一次 `Infer` 的所有读取基于同一快照 |
| 10 | **集合值派生**（`V-1`）：一条规则推出 N 个事实时，能回答"这条 `via` 覆盖哪几个成员"；成员清单 **必须**与实际结果同源，不得另行计算 |
| 11 | **否定性派生**（`V-2`）：构造一个"未命中任何规则"的输入，`via.premises` **必须**非空且为否定谓词；校验器 **必须**接受它 |
| 12 | **反例（防伪造）**：把否定事实伪造成一条正向前提，校验 **必须**失败 —— 这是样本 2 的真正价值所在 |
| 13 | **校验器不得有兜底放行**：未知关系名 / 未知谓词 **必须**判为"不存在"，**不得**默认通过 |

> 样本 13 值得单独列：参考实现的第一版探针给未知关系留了个 `: true` 的兜底，
> 于是**任何编造的前提都被判为"真实"**，整套校验形同虚设。
> **校验器的漏洞比被校验对象的漏洞更危险**——它会让所有测试都变绿。

---

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09 | 初稿。定义最小 I3，首次实现 `derived_by` 的求值与 `via` 溯源 |
| **v0.2** | 2026-09 | **新增 §3.2.1**：补上规范未覆盖的两种派生形态 —— `V-1` **集合值派生**（一条规则推一组事实，"一条事实一条 via"的默认不成立）、`V-2`/`V-2a` **否定性派生**（从"没有匹配"得出结论时，MUST 显式表达否定事实，MUST NOT 伪造正向前提）。§7 增补样本 10~13，其中样本 13「校验器不得有兜底放行」来自一次真实事故：探针对未知关系留了 `: true` 兜底，导致任何编造的前提都被判为真实 |

---

## 许可

[Apache License 2.0](../LICENSE)
