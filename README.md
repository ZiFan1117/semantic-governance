# 语义治理框架（Semantic Governance Framework）

> **让组织里的每一个系统、每一个人、每一个智能体，都在同一套语义背景上说话。**

**状态**：草案 v0.3 · 征求意见中（RFC）
**许可**：Apache-2.0

---

## 这是什么

一套**开放、通用**的框架，规定：

- 业务语义如何被**声明**（五要素：对象、关系、规则、策略、动作）
- 语义如何被**治理**（谁有权定义、怎么改、有分歧怎么办、怎么退出）
- 系统之间靠**什么契约**对话（六个契约，各自的保证）
- 一套实现**做到哪一步算合规**（五级阶梯）

它要解决的问题很具体：

> 同一个"客户"在四个系统里四个定义，
> 同一个"营收"在四个看板里四个数字，
> AI 拿到互相冲突的业务逻辑只能胡说，
> 想改一个规则要改几十个地方，还改不全。

**Palantir 的 Ontology 解决了这个问题，但它是专有的。**
**本框架给出一个通用、公开、可开源的等价物。**

---

## 核心理念：语义治理，不是语义建模

| 词 | 隐含的重心 | 问题 |
|---|---|---|
| 本体论 | 建模 | 听起来像学术活动，做完就完了 |
| 知识图谱 | 数据 | 听起来像存储选型 |
| **语义治理** | **权责 + 契约 + 技术** | ✅ 点出了"这是一件持续治理的事" |

**"治理"三个字要求框架必须回答：谁有权定义、怎么改、有分歧怎么办、怎么退出。**

一个不回答这些问题的"语义"框架，只是又一份 schema 规范。

---

## 核心命题

> **把业务语义从应用代码里抽出来，做成一份可声明、可校验、可执行、可交换的权威契约；
> 规定六个契约，让所有读写都必须经过它；
> 并用一套治理机制保证这份契约能持续演进而不失控。**

---

## 形态：中央式权威，以及它的代价

本框架采用**中央式**——一套权威语义，所有系统向它对齐。

**它给你什么**：强一致、无歧义、可治理、可推理。

**它要你放弃什么**（这一条必须说清）：

- ❌ "任何人都能改核心" —— 不可能
- ❌ 域之间的完全自治 —— 各域必须向核心对齐
- ⚠️ 中央成为瓶颈、成为风险点

**所以本框架的"开放"具体指什么：**

| ✅ 开放的 | ❌ 不开放的 |
|---|---|
| 规范本身（任何人可自由实现） | **核心定义的直接修改权** |
| **提案权**（任何人可提变更提案） | |
| 扩展命名空间（域内自定义，无需批准） | |
| **分叉权**（可 fork 核心，另立门户） | |
| **退出权**（可导出全部定义与事实并离开） | |

> **开放的是「加入、提案、分叉、退出」，不是「就地修改」。**
>
> 这不是妥协——IETF、W3C、Kubernetes 都是这个形态：
> **提案开放，权威集中**。这是唯一被验证过能同时做到"开放"和"稳定"的形态。

---

## 六个契约（框架的核心）

标准化不是靠分层，是靠**接口契约**。

| # | 契约 | 保证核心 | 现有标准 |
|---|---|---|---|
| **I1** | **语义契约** | 读定义，定义不可变 | ✅ **Apache Ossie** |
| **I2** | **事实契约** | **写入必须过规则** | ⚠️ GQL（需补一条） |
| **I3** | **查询契约** | **结果必须可追溯** | ⚠️ MCP（需补一条） |
| **I4** | **动作契约** | 受控修改的完整契约 | ❌ **需原创** |
| **I5** | **接入契约** | 契约先于数据 | ✅ **ODCS** |
| **I6** | **发现契约** | 不知道有什么时能列出来 | ❌ **需原创** |

> **要发明的只有两个（I4、I6），要补的只有两条约束（I2-1、I3-2），
> 其余全部是选型与组合。**
>
> 这不是缺点。标准之所以能成，往往不是因为它发明了什么，
> 而是因为它**把已有的东西拼成了一个可用的整体**。（schema.org 就是这么成的。）

---

## 与现有标准的关系：不重复造轮子

| 已有标准 | 覆盖什么 | 本框架 |
|---|---|---|
| **Apache Ossie** | 语义定义的格式 | ✅ 直接采用 |
| **ODCS** | 数据契约 | ✅ 直接采用 |
| **GQL**（ISO/IEC 39075:2024） | 图查询语言 | ✅ 直接采用 |
| **MCP** | Agent 接入 | ✅ 直接采用 |
| **OpenFGA / Zanzibar** | 授权模型 | ✅ 直接采用 |
| **OpenLineage** | 数据血缘 | ✅ 直接采用 |
| **CloudEvents** | 事件格式 | ✅ 直接采用 |
| **W3C PROV-O** | 溯源 | ✅ 参考 |
| **OWL / SHACL** | 形式化本体与校验 | ⚠️ 按需补充，不作主格式 |
| **Palantir Ontology** | 全套（专有） | ⚠️ **抄概念，不抄实现** |

---

## 文档

### 规范（Normative）

| 文档 | 内容 | 状态 |
|---|---|---|
| **[spec/module-discipline.md](spec/module-discipline.md)** | **模块纪律** —— 原子化 · C1–C4 · 构建期门禁 · 渐进披露 · 判据 · 真源与派生视图 | v0.1 |
| **[spec/i4-action-minimal.md](spec/i4-action-minimal.md)** | **I4 动作契约** —— 单实例→多实例 · 同步 · 有审计 | v0.2.4 |
| **[spec/i3-query-minimal.md](spec/i3-query-minimal.md)** | **I3 查询契约** —— 模式查询 · 遍历 · **派生求值与 `via` 溯源** | v0.2 |
| **[spec/i6-discovery-minimal.md](spec/i6-discovery-minimal.md)** | **I6 发现契约** —— 枚举 · 描述 · 按需查找 | v0.1 |

### 框架与背景

| 文档 | 内容 |
|---|---|
| **[docs/framework.md](docs/framework.md)** | **主体**：理念、治理、**五要素**（对象·关系·规则·策略·动作）、**四个正交维度**（要素/角色/格式/契约）、六个契约、纪律条款、合规等级、规范条款 |
| [docs/rationale.md](docs/rationale.md) | **背景分析**：为什么这么设计——与知识图谱/Palantir 的对比、约束与推理机制、语义网失败的教训 |
| [docs/open-questions/](docs/open-questions/) | 9 条开放问题的逐条展开（对应 Issues） |

### 本仓库的边界：只出规范，不出软件

**交付物是"一套别人得照着实现的定义"，不是实现。**

| 有 | 没有 |
|---|---|
| 规范条款（`MUST` / `MUST NOT` / `SHOULD`），每条都可验收 | ❌ 参考实现代码 |
| 三种剖面与五级合规等级：**声明你实现到哪一级** | ❌ 一致性测试套件 |
| 治理机制：提案、评审、弃用、分叉、退出 | ❌ 任何可执行产物 |
| **规范自检闸**（[tools/audit.py](tools/audit.py)）：锚点 · 引用 · 条款编号 · 编码 | |

**为什么不留实现**：一份实现会**被误当成标准**——实现里没做的，读者会以为规范也没要求。
规范要求读者自己判断"我做到哪一级了"，而不是对着别人的代码抄。

### 规范自身的门禁

规范也是文档，也会烂：改了正文忘改引用、条款编号撞号、编码损坏。
所以本仓库**以身作则**，把审计固化成一条命令（离线、失败返回非零退出码）：

```bash
python tools/audit.py
```

它检查相对链接、标题锚点、`§N` 节引用、**条款编号前缀的唯一归属**、编码、已删产物残留。
**改完任何文档都应该跑一遍。** 详见 [tools/README.md](tools/README.md)。

> **溯源说明**：本框架的若干条款是从**真实实现反馈**里订出来的（哪些声明落不了地、哪些强制点会漏）。
> 这些条款与它们的理由**全部保留在规范正文里**（正文中标注为"来自实现反馈"），
> 但**对应的实现代码与测试套件已从仓库移除**——早期版本见 git 历史。

### 参与

| 文档 | 内容 |
|---|---|
| [CONTRIBUTING.md](CONTRIBUTING.md) | 变更提案流程（本框架自己要求提案制，所以这里以身作则） |
| [proposals/](proposals/) | 变更提案 |
| [Issues](https://github.com/ZiFan1117/semantic-governance/issues) | 开放问题讨论 |

---

## 三种剖面（Profile）

不同用途的需求差别极大，所以框架定义三种剖面，而不是一套统一要求：

| | **A · 轻量** | **B · 组织** | **C · 严格** |
|---|---|---|---|
| 适用 | Agent 记忆库 | 部门数据治理 | 跨系统权威语义 |
| 变更流程 | 无需提案 | 内部评审 | 正式提案 + 公示 + 弃用期 |
| 目标等级 | L0~L1 | L0~L3 | L0~L4 |

---

## 五级合规阶梯

```
Level 4  可运营   + 全部切面（合规删除、成本、SLA、治理流程）
   ↑
Level 3  可治理   + 授权 + 审计 + 版本
   ↑
Level 2  可写     + 动作接口              ← ⭐ 分水岭
   ↑
Level 1  可读     + 事实 + 查询 + 发现契约
   ↑
Level 0  可声明   + 语义接口                ← Ossie 已给
```

> **Level 1 → 2 是分水岭：以下只能"看"，以上才能"安全地改"。**
> **绝大多数开源方案停在 Level 1。**

---

## 如何参与

**本框架自己要求提案制，所以这里以身作则。**

1. **提 Issue 讨论** —— 任何问题、质疑、场景
2. **提变更提案** —— 用 [proposals/0000-template.md](proposals/0000-template.md) 模板
3. **分叉自由** —— 不满意就直接 fork，Apache-2.0 允许任何用途

**特别欢迎的反馈：**

- 🎯 **反驳**：这个框架哪里错了？（见 framework.md §28 的自批判）
- 🎯 **场景**：你打算用它做什么？哪一层不适用？
- 🎯 **实现**：你实现到哪一级了？卡在哪？

---

## 已知的开放问题

框架**不是完整答案**。最重要的两条自我质疑（详见 framework.md §28）：

> **1. 治理真正难的是让人达成共识。**
> 一个高效的提案系统也救不了一群谈不拢的人。
>
> **2. 采用本框架 = 选择"强 TBox"路线。**
> 如果你的场景里语义稳定、消费者很少，那用一张表 + 一段代码就够了，**不要用这个框架**。

其余 7 条开放问题包括：派生不落库与前置条件的冲突、动作抽象模型无先例、身份解析的准确率上限、合规删除的完整性验证、定义变更的影响分析等。

---

## 致谢

本框架站在这些工作之上：

- [Apache Ossie](https://ossie.apache.org/)（原 Open Semantic Interchange）——语义定义的格式
- [Palantir Ontology](https://www.palantir.com/docs/foundry/ontology/overview/)——语义层 + 动能层的概念
- Google [Zanzibar](https://research.google/pubs/pub48190/) 论文——授权模型
- W3C 语义网、OWL、SHACL——形式本体的理论与教训
- [Semantica](https://github.com/semantica-agi/semantica)、[dsh-ontology](https://github.com/tancheng33/dsh-ontology)——生态中的实践参照

---

## 许可

[Apache License 2.0](LICENSE)
